// import {describe, it} from 'mocha';
import { expect } from 'chai';

describe("Dummy tests to prove this works", () => {
   it('should assert true', () => {
       expect(true).to.equal(true);
   }) 
});
